/**
 * Esqueleto de la primera entrada a la aplicación (por ejemplo, justo después
 * de iniciar sesión) y de las pantallas de detalle que no tienen el suyo.
 *
 * Para las navegaciones ENTRE secciones no sirve —esta frontera ya está
 * montada y React conserva el contenido anterior—; de eso se encarga el
 * `loading.tsx` de cada segmento.
 */
export { default } from "@/components/esqueleto-seccion";
